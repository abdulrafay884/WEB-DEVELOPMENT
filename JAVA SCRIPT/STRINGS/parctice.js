// ? generate a method of developing username by entering fullname and add @ in the starting and name length in the end

// ! let fname = prompt("ENTER FULL NAME: ")
// ! let uname = (`your username is @${fname}${fname.length}`).toUpperCase();
// ! alert(uname)
// ! console.log(uname);

